export type ExtraClasses = {
    [key: string]: string
}

export enum BoxColor {
    Red = "red",
    Green = "green"
}

export enum SocialIcons {
    Github = "github",
    Email = "email",
    Mailchimp = "mailchimp",
    Instagram = "instagram",
    Discord = "discord"
}